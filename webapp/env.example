# ============================================================================
# LOCAL DEVELOPMENT - Faker Stories
# ============================================================================
# Content directory path (relative to webapp directory)
CONTENT_DIR=../stories

# ============================================================================
# PRODUCTION DEPLOYMENT - GitHub Stories (Runtime)
# ============================================================================
# Required for production (Vercel)
# Fetches published stories from your PUBLIC GitHub repository at RUNTIME
# No API key needed (public repository)

# Your GitHub username
NEXT_PUBLIC_GITHUB_OWNER=your-github-username

# Your stories repository name
NEXT_PUBLIC_GITHUB_REPO=travel-journal-stories

# Branch to fetch from (default: main)
NEXT_PUBLIC_GITHUB_BRANCH=main

# ============================================================================
# HOW IT WORKS
# ============================================================================
# Development:
#   → Reads faker stories from ../stories folder
#   → No network calls, instant loading
#
# Production:
#   → Fetches published stories from GitHub at runtime
#   → Only non-draft, non-archived stories
#   → Cached for 60 seconds (ISR)
#   → No build/deploy needed for new stories!
#
# ============================================================================
# NOTES
# ============================================================================
# - Stories appear immediately after push to GitHub (no rebuild needed)
# - Repository must be public (or add GITHUB_TOKEN for private repos)
# - Uses Next.js ISR (Incremental Static Regeneration) for caching
