# Non-secret configuration values
# These values are safe to commit and share

# API service endpoint (deployed on Vercel)
EXPO_PUBLIC_API_BASE_URL=https://travel-journal-lyart-kappa.vercel.app

# CloudFront CDN domain for serving uploaded media
EXPO_PUBLIC_CLOUDFRONT_DOMAIN=dkt41sp0pphbb.cloudfront.net

# Environment Behavior Notes:
# - Development mode (__DEV__ = true):
#   - GitHub stories publish to 'dev-playground' branch
#   - Media uploads go to 'dev/' prefix in S3 (handled by API)
# - Production mode (__DEV__ = false):
#   - GitHub stories publish to 'main' branch  
#   - Media uploads go to 'prod/' prefix in S3 (handled by API)
